/**
 * Shared canvas settings for every composition in this project.
 * Change these two lines to switch the whole project to 4K / 60fps —
 * every composition in src/compositions/ reads from here instead of
 * hardcoding its own dimensions.
 */
export const VIDEO_WIDTH = 1920;
export const VIDEO_HEIGHT = 1080;
export const VIDEO_FPS = 30;
